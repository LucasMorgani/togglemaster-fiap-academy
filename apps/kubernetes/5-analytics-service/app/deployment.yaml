apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: app-analytics-service
  name: app-analytics-service
  namespace: analytics-service
spec:
  strategy:
    rollingUpdate:
      maxSurge: 50%
      maxUnavailable: 0
  replicas: 2
  selector:
    matchLabels:
      app: app-analytics-service
  template:
    metadata:
      labels:
        app: app-analytics-service
    spec:
      containers:
        - name: app-analytics-service 
          image: morganis1411/togglemaster_analytics-service:1.1
          resources:
            requests:
              cpu: "250m"
              memory: "206Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          livenessProbe:
            httpGet:
              path: "/health"
              port: 8005
          readinessProbe:
            httpGet:
              path: "/health"
              port: 8005
          envFrom:
            - secretRef:
                name: analytics-db-secret #Credentials to db login and generics
            - secretRef:
                name: analytics-db-secret-host #Credential to db login (host)